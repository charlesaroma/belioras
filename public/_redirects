# The proxy must come before the SPA fallback, or /api calls are answered
# with index.html. Inert until the API is deployed; nothing calls /api yet.
/api/*  https://api.belioras.com/api/:splat  200!
/*      /index.html                          200
